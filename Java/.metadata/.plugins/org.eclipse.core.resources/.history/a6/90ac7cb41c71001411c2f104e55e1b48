import static org.lwjgl.opengl.GL11.*;

public class OpenGLHelper 
{
	public static void openGLErrorCheck()
	{
		int error = glGetError();
		switch (error)
		{
		case NO_ERROR: return;
		case INVALID_ENUM: throw new IllegalArgumentException("Invalid OpenGL enumerated argument.");
		case INVALID_VALUE: throw new IllegalArgumentException("A numeric OpenGL argument is out of range.");
		}
	}
}
